/**
 * Her OTA yayınında ELLE değiştirilen sürüm etiketi.
 *
 * `Updates.updateId` / `isEmbeddedLaunch` platforma göre farklı davranabiliyor;
 * bu etiket yalnızca JS bundle'ıyla birlikte değiştiği için "doğru sürüm indi
 * mi?" sorusunun tartışmasız cevabı. Arabaya çıkmadan Bus ekranına bakıp
 * beklenen değerle karşılaştırmak yeterli.
 */
export const JS_BUILD_TAG = '2026-09-11 · js-33 (debug into link, cycle preflight)';
